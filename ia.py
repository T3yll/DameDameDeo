import generationDico as gd

def evalMoves(pionList, gameBoard):
    moves_dict = gd.generate_dames_moves()
    #for pionList
    #if pionSelected in PionList can move
    #for moves_dict 
        #if moves_dict.getCoords[j] == moves_dict[i].pionSelected.getCoords + 1 row
            #moves_dict.setPointToCoords[j] = 1
            #newCoords = moves_dict.getCoords[j] + 1 row
            #if newCoords.theresAllyDevantOuDerriere
                ##moves_dict.setPointToCoords[j] += 1
    #best_moves list = moves_dict.getBestCoords
    #next_moveString = best_moves.random()
    #for pionList